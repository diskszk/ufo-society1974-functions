export const DRAFT_ALBUMS = "albums" as const;
export const PUBLISHED_ALBUMS = "published_albums" as const;
export const PUBLISHED_DATE = "publishedDate" as const;
export const SONGS = "songs" as const;
export const USERS = "users" as const;

export const role = {
  MASTER: "master",
  EDITOR: "editor",
  WATCHER: "watcher",
} as const;
