export const ALBUMS = "albums" as const;
export const DRAFT_ALBUMS = "draft-albums" as const;
export const PUBLISHED_DATE = "publishedDate" as const;
export const SONGS = "songs" as const;
export const USERS = "users" as const;

export const role = {
  MASTER: "master",
  EDITOR: "editor",
  WATCHER: "watcher",
} as const;
